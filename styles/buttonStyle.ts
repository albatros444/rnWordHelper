import { StyleSheet } from "react-native";
export const buttonStyle = StyleSheet.create({
  buttonText: {
    color: "gray",
    fontSize: 20,
    textAlign: "center",
  },
  button: {
    height: 50,
    backgroundColor: "#ff7",
    paddingVertical: 7,
    borderRadius: 10,
    justifyContent: "center",
    alignItems: "center",
  },
});
